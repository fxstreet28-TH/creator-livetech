# Fullscreen gift layer — anchored positions

The tier 03–07 stage after it was moved onto the bottom edge of its surface.

## Desktop / creator studio, 1280

From `/dev/gifts`, which mounts the same `GiftOverlay` in the same
relatively-positioned 16:9 box the creator studio and the desktop viewer give
it, so the derived anchored layout resolves identically (892 × 502 player →
282px stage). The studio itself needs a camera and a live session; the bench is
the reproducible stand-in.

| file | what it shows |
| --- | --- |
| `1280-studio-nova-with-tray.jpg` | Nova (CSS tier, square) on the floor, caption above it, a Stardust row beside it on the same bottom line |
| `1280-studio-tier07-with-tray.jpg` | a tier-07 video card (720 × 476) in the same arrangement — the widest thing the stage can hold, with the centre of frame still clear |

Measured on the captures: stage bottom 24px above the player's bottom edge,
tray row bottom on the same line, caption 8px above the stage and flush with
its left edge, stage 282px (was 320).

## Phone viewer

Moved to `../orientation/`, which covers both ways up.
